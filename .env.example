# Clave de API de OpenAI (platform.openai.com). Requerida por app/api/analizar/route.ts.
# Nunca se expone al cliente: solo se lee del lado del servidor.
OPENAI_API_KEY=

# Opcionales. Los valores por defecto están en el código.
# Límite de caracteres de la nota (por defecto 40000).
LONGITUD_MAXIMA_NOTA=
# Límite de peticiones por IP y ventana (por defecto 5 análisis / 20 reanálisis cada 10 min).
LIMITE_ANALISIS_POR_VENTANA=
LIMITE_REANALISIS_POR_VENTANA=
LIMITE_VENTANA_MS=
# Semilla para la llamada a OpenAI, para evals comparables entre ejecuciones (por defecto 42).
OPENAI_SEED=
# Pasada crítica: segunda llamada opt-in con gpt-4o-mini que revisa el informe
# ya generado (ver lib/pasadaCritica.ts). Duplica el coste por análisis
# completo. Desactivada por defecto; poner "true" para activarla.
PASADA_CRITICA=
